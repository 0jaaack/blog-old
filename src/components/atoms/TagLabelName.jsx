import styled from "styled-components";

const TagLabelName = styled.span`
  color: ${(({ theme }) => theme.colors.orange)};
  font-size: 0.8rem;
  font-weight: 400;
  text-transform: uppercase;
`;

export default TagLabelName;
